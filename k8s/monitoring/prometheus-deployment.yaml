apiVersion: v1
kind: ConfigMap
metadata:
  name: prometheus-config
  namespace: pidev
data:
  prometheus.yml: |
    global:
      scrape_interval: 15s
    scrape_configs:
      - job_name: 'certification-service'
        metrics_path: '/actuator/prometheus'
        static_configs:
          - targets: ['certification-service:8090']
      - job_name: 'mentor-service'
        metrics_path: '/actuator/prometheus'
        static_configs:
          - targets: ['mentor-service:8088']
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: prometheus
  namespace: pidev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: prometheus
  template:
    metadata:
      labels:
        app: prometheus
    spec:
      containers:
        - name: prometheus
          image: prom/prometheus:latest
          ports:
            - containerPort: 9090
          volumeMounts:
            - name: config
              mountPath: /etc/prometheus/prometheus.yml
              subPath: prometheus.yml
      volumes:
        - name: config
          configMap:
            name: prometheus-config
---
apiVersion: v1
kind: Service
metadata:
  name: prometheus-service
  namespace: pidev
spec:
  selector:
    app: prometheus
  ports:
    - port: 9090
      targetPort: 9090
      nodePort: 30090
  type: NodePort
